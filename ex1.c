#include<stdio.h>
int main()
{
int n;
scanf("%d",&n);
if(n==0)
printf("\n The given number is zero");
else if(n>0)
printf("\n The given number is positive");
else
printf("\n The given number is negative");
return 0;
}
